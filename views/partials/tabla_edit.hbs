{{#if tabla_edit}}
<div class="table-responsive">
    <table class="table table-striped table-hover table-sm">
        {{#each tabla_edit}}
        {{#if @first}}
        <thead>
            <tr>
                {{#each this}}
                <th>{{@key}}</th>
                {{/each}}
                {{!-- opcion --}}
                {{!-- <th>Opcion</th> --}}
            </tr>
        </thead>
        {{/if}}
        {{/each}}
        <tbody class="">
            {{#each tabla_edit}}

            {{{crear_tr this}}}

            {{!-- <tr tb-id="{{this.id}}"> --}}
                   
                {{!-- {{#each this}} --}}
                {{!-- <td class="{{@key}} module">{{this}}</td> --}}
                {{!-- {{{filtrar_td @key this this.this}}}  --}}
                 
                {{!-- {{/each}} --}}


                {{!-- opciones --}}
                {{!-- <td>
                    <div class="btn-group">
                        <form action="" method="POST" >
                            <input type="hidden" value="{{this.id}}" name="campoId">
                            <input type="hidden" value="edit" name="accion">
                            <button type="submit" class="btn btn-sm btn-outline-warning">Editar</button>
                        </form>
                        <form action="" method="POST" >
                            <input type="hidden" value="{{this.id}}" name="campoId">
                            <input type="hidden" value="delete" name="accion">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Eliminar</button>
                        </form>
                        {{!-- <button type="submit" class="btn btn-sm btn-outline-danger">Eliminar</button> --}}
                    </div>
                </td> --}}
            {{!-- </tr> --}}
            {{/each}}

        </tbody>
    </table>
</div>
{{/if}}